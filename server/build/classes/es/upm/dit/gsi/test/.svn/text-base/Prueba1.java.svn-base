/*package es.upm.dit.gsi.test;

import com.meterware.httpunit.GetMethodWebRequest;
import com.meterware.httpunit.WebRequest;
import com.meterware.httpunit.WebResponse;
import com.meterware.servletunit.ServletRunner;
import com.meterware.servletunit.ServletUnitClient;
import junit.framework.TestCase;

import es.upm.dit.gsi.connection.Configuration;
import es.upm.dit.gsi.servlet.Mahout;

public class Prueba1  extends TestCase {*/
	
	/*public void testGetRecommendation(){
		Configuration.setTestConfiguration("mahouttest", "mahoutup", "mahoutpp");
		ServletRunner sr = new ServletRunner();
        sr.registerServlet("Mahout", Mahout.class.getName());
        ServletUnitClient sc = sr.newClient();
        WebRequest req = new GetMethodWebRequest("http://localhost:8080/Mahout?action=getRecommendation&identifier=adri&max_recom=1");  
        try {
        	WebResponse response = sc.getResponse(req);
        	System.out.println(response.getText());
        	assertEquals("Nada de nada", response.getText());
		} catch (Exception e) {
			e.printStackTrace();
		}
	}*/
	
	/*public void testSetPreference(){
		Configuration.setTestConfiguration("mahouttest", "mahoutup", "mahoutpp", "jdbc:mysql://localhost:3306/mahouttest");
		ServletRunner sr = new ServletRunner();
        sr.registerServlet("Mahout", Mahout.class.getName());
        ServletUnitClient sc = sr.newClient();
        WebRequest req = new GetMethodWebRequest("http://localhost:8080/Mahout?action=setPreference&identifier=adri&item=lourdes&preference=10.0");  
        try {
        	WebResponse response = sc.getResponse(req);
        	System.out.println(response.getText());
        	assertEquals("Nada de nada", response.getText());
		} catch (Exception e) {
			e.printStackTrace();
		}
	}*/
	
	
	
	/*public void testXXX()
	{
	    // Initialize class to test
	    Mahout recomendador = new Mahout();
	    
	    // Set a variable in session as the doSomething() method that we are testing need
	    // this variable to be present in the session (for example)
	    session.setAttribute("name", "value");

	    // Call the method to test, passing an HttpServletRequest object (for example)
	    String result = recomendador.doGet("http://localhost:8080/Mahout?action=getRecommendation&identifier=adri&max_recom=1",Response res);

	    // Perform verification that test was successful
	    assertEquals("something", result);
	    assertEquals("otherValue", session.getAttribute("otherName"));
	}*/


//}
